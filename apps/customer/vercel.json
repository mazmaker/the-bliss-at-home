{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "ignoreCommand": "git diff --quiet $VERCEL_GIT_PREVIOUS_SHA HEAD -- apps/customer packages/ 2>/dev/null; code=$?; [ $code -eq 128 ] && exit 1 || exit $code"
}
