#!/bin/bash
echo "🔄 กำลังอัพเดท TypeScript types..."
supabase gen types --lang=typescript --project-id=rbdvlfriqjnwpxmmgisf > packages/types/database.types.ts
echo "✅ อัพเดท types เสร็จแล้ว!"

# ตรวจสอบว่าคอลัมน์ถูกเพิ่มแล้ว
if grep -q "discount_rate" packages/types/database.types.ts; then
  echo "🎉 สำเร็จ! คอลัมน์ discount_rate ถูกเพิ่มแล้ว"
  echo "💻 ตอนนี้คุณสามารถแก้ไขโรงแรมและเพิ่มอัตราส่วนลดได้แล้ว!"
else
  echo "❌ ยังไม่พบคอลัมน์ discount_rate - กรุณาตรวจสอบว่ารัน SQL ใน Supabase Dashboard แล้ว"
fi
