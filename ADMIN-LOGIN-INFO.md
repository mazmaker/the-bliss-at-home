# Admin Login Information
## ข้อมูลการเข้าสู่ระบบ Admin Dashboard

### 🔐 ข้อมูลเข้าสู่ระบบปัจจุบัน

**URL**: http://localhost:3001/admin/login

**สำหรับ Production (Supabase Auth จริง):**
- Email: `admin@bliss.com`
- Password: `admin123456`

**สำหรับ Development (Mock Auth):**
- Email: `admin@theblissathome.com`
- Password: `admin123456`

### 📝 หมายเหตุ

1. **ปัจจุบันใช้ Real Auth Mode** (VITE_USE_MOCK_AUTH=false)
   - ใช้ email: `admin@bliss.com` ในการเข้าสู่ระบบ
   - เชื่อมต่อกับ Supabase Cloud จริง

2. **สถานะ Email:**
   - `admin@bliss.com` - ✅ ใช้งานได้ปกติ (ใช้ตัวนี้)
   - `admin@theblissathome.com` - ❌ มีปัญหาใน Auth system

3. **การสลับโหมด:**
   ```bash
   # สลับเป็น Mock Auth (Development)
   # แก้ไขใน apps/admin/.env.local
   VITE_USE_MOCK_AUTH=true

   # สลับเป็น Real Auth (Production)
   VITE_USE_MOCK_AUTH=false
   ```

### 🛠️ วิธีแก้ไขหากเข้าสู่ระบบไม่ได้

1. **ตรวจสอบ Mode:**
   ```bash
   cat apps/admin/.env.local | grep VITE_USE_MOCK_AUTH
   ```

2. **ถ้าใช้ Real Auth แต่เข้าไม่ได้:**
   - ลองใช้ `admin@bliss.com`
   - หรือเปลี่ยนเป็น Mock Auth ชั่วคราว

3. **ถ้าใช้ Mock Auth:**
   - ใช้ `admin@theblissathome.com`
   - ตรวจสอบว่า VITE_USE_MOCK_AUTH=true

### 🔄 การอัปเดต Email ในอนาคต

เมื่อต้องการเปลี่ยน email เป็น `admin@theblissathome.com` อย่างสมบูรณ์:

1. แก้ไขปัญหา Auth system ใน Supabase
2. อัปเดต auth.users table
3. อัปเดต profiles table
4. ทดสอบการเข้าสู่ระบบ

---

*อัปเดตล่าสุด: 23 มกราคม 2026*