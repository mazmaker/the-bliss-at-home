# แนะนำการแก้ปัญหา Hotel Login

## 🚨 ปัญหาปัจจุบัน
- อีเมล `sweettuay.bt@gmail.com` เชื่อมกับ "โรงแรมฮิลตัน" แทน "รีสอร์ทในฝัน เชียงใหม่"
- Server ไม่สามารถเชื่อมต่อ Supabase ได้

## 💡 วิธีแก้ไขด่วน (แนะนำ)

### ใช้ข้อมูลที่มีอยู่แล้ว:

1. **ดูอีเมลที่ส่งไปแล้ว:**
   - ไปที่: https://ethereal.email
   - Username: `tq246htcvwfhnoht@ethereal.email`
   - Password: `TKf9XywYhWVMW6JmVZ`
   - หาอีเมลที่ส่งไปยัง `manager@dreamresortchiangmai.com`

2. **ข้อมูลการเข้าสู่ระบบจากอีเมล:**
   - URL: http://localhost:3006/login
   - อีเมล: `manager@dreamresortchiangmai.com`
   - รหัสผ่าน: `Test123456!` (หรือตามที่แสดงในอีเมล)

## 🛠️ วิธีแก้ไขระบบ (สำหรับการใช้งานจริง)

### 1. ตั้งค่า Local Supabase
```bash
# ติดตั้ง Docker (หากยังไม่มี)
# แล้วรัน
supabase start
```

### 2. อัพเดท .env
```bash
# apps/server/.env
SUPABASE_URL=http://localhost:54321
SUPABASE_SERVICE_ROLE_KEY=<service-role-key-from-supabase-start>
```

### 3. Import Mock Data
```bash
# รัน SQL script
supabase db reset
```

## 📋 ข้อมูล Mock Hotels

จากไฟล์ `backup_migrations/20240201000001_insert_hotels_mock_data.sql`:

1. **โรงแรมฮิลตัน กรุงเทพฯ** (ID: 440001)
   - อีเมล: `reservations@hilton.com`

2. **รีสอร์ทในฝัน เชียงใหม่** (ID: 440002) ✅
   - อีเมล: `booking@nimman.com`
   - ควรจะเป็น: `manager@dreamresortchiangmai.com`

3. **โรงแรมดุสิต ธานี** (ID: 440003)
   - อีเมล: `info@dusit.com`

## ✅ การทดสอบที่สำเร็จแล้ว

✅ ระบบส่งอีเมลทำงานได้
✅ Email Service สร้าง Test Account ได้
✅ Template อีเมลสวยงามครบถ้วน
✅ ส่งอีเมลไปยัง `manager@dreamresortchiangmai.com` แล้ว

## 🎯 ขั้นตอนถัดไป

1. **ตรวจสอบอีเมลใน Ethereal Email** เพื่อเอาข้อมูลการล็อกอิน
2. **ทดสอบล็อกอินด้วยข้อมูลจากอีเมล**
3. **หากล็อกอินไม่ได้ ให้ตั้งค่า Local Supabase**