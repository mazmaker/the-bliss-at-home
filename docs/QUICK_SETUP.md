# ⚡ Quick Setup Guide - Documents Management

คำแนะนำติดตั้งอย่างรวดเร็ว (5-10 นาที)

## 🚀 ขั้นตอนที่ 1: Database (2-3 นาที)

1. เปิด Supabase Dashboard → SQL Editor
2. รันไฟล์ทีละไฟล์:

```bash
# ไฟล์ที่ 1
packages/supabase/migrations/20250205_create_staff_documents.sql

# ไฟล์ที่ 2
packages/supabase/migrations/20250205_staff_documents_rls.sql
```

3. ✅ ตรวจสอบว่าตาราง `staff_documents` ถูกสร้างแล้ว

---

## 📦 ขั้นตอนที่ 2: Storage (2 นาที)

1. ไปที่ Supabase → Storage → Create bucket
2. ตั้งค่า:
   - Name: `staff-documents`
   - Public: ❌ (Private)
   - File size: `10 MB`
   - MIME: `image/jpeg, image/png, application/pdf`

3. ไปที่ Policies → New Policy → Custom
4. คัดลอก policies จาก guide (หรือใช้ default ไปก่อน)

---

## 🔔 ขั้นตอนที่ 3: LINE Notify (1-2 นาที) - Optional

1. ไปที่ https://notify-bot.line.me/
2. Login → My page → Generate token
3. ตั้งชื่อ: "The Bliss Admin"
4. คัดลอก token

---

## ⚙️ ขั้นตอนที่ 4: Environment Variables (1 นาที)

เพิ่มใน `.env`:

```env
# Admin App
VITE_LINE_NOTIFY_TOKEN_ADMIN=your_token_here
```

---

## ✅ ขั้นตอนที่ 5: ทดสอบ (1-2 นาที)

1. รัน dev server:
```bash
npm run dev
```

2. เปิด: http://localhost:3007/admin/staff

3. คลิกดูพนักงานคนใดก็ได้

4. แท็บ "เอกสาร KYC" → "เพิ่มเอกสาร"

5. อัปโหลดไฟล์ทดสอบ

6. ✅ ถ้าอัปโหลดสำเร็จ = Setup เสร็จแล้ว!

---

## 🐛 แก้ปัญหาด่วน

### อัปโหลดไม่ได้
- ตรวจสอบว่า bucket `staff-documents` ถูกสร้าง
- ตรวจสอบ file size < 10MB

### ไม่เห็นเอกสาร
- ตรวจสอบ Console (F12)
- ตรวจสอบ RLS policies

### LINE ไม่ส่ง
- ตรวจสอบ token ใน .env
- Restart dev server

---

## 📚 เอกสารเพิ่มเติม

ดูรายละเอียดเพิ่มเติมที่: [DOCUMENTS_SETUP_GUIDE.md](./DOCUMENTS_SETUP_GUIDE.md)

---

**หากติดปัญหา:**
1. ดู Console logs
2. ดู Supabase logs
3. ติดต่อ Dev team
