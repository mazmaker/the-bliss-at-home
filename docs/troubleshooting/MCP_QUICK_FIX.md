# MCP Quick Fix Guide

เมื่อเปิดเครื่องใหม่แล้ว MCP ไม่ทำงาน ให้ทำตามนี้:

## 🚀 Quick Start (3 นาที)

### 1. ตรวจสอบสถานะ

```bash
bash check-mcp.sh
```

### 2. แก้ไขตามที่ script บอก

ถ้า script บอกว่า package ไม่พบ:

```bash
# Context7
npm install -g @upstash/context7-mcp

# shadcn-ui
npm install -g @jpisnice/shadcn-ui-mcp-server
```

### 3. Reload Claude Code

- กด `Ctrl+Shift+P`
- พิมพ์ "Reload Window"
- กด Enter

### 4. ทดสอบอีกครั้ง

ถามใน Claude Code:
```
ตรวจสอบให้หน่อยว่ามีการเชื่อมการทำงานกับ mcp อะไรบ้าง และแต่ละตัวพร้อมใช้งานไหม
```

---

## ⚡ One-Click Fix

หากต้องการแก้ไขทุกอย่างในคำสั่งเดียว:

```bash
# ติดตั้ง packages ทั้งหมด
npm install -g @upstash/context7-mcp @jpisnice/shadcn-ui-mcp-server

# ตรวจสอบสถานะ
bash check-mcp.sh
```

จากนั้น Reload Claude Code

---

## 🔴 ปัญหาที่พบบ่อย

### Playwright MCP หายจาก .mcp.json

**อาการ:** มี 3 MCP เท่านั้น ไม่มี playwright

**วิธีแก้:** เพิ่มเข้าไปใน `.mcp.json`

```json
{
  "mcpServers": {
    // ... servers อื่นๆ
    "playwright": {
      "command": "npx.cmd",
      "args": [
        "-y",
        "@executeautomation/playwright-mcp-server@latest"
      ],
      "env": {
        "PLAYWRIGHT_MCP_DATA_DIR": ".playwright-mcp"
      }
    }
  }
}
```

### Context7/shadcn-ui ไม่ทำงานหลัง update Node.js

**อาการ:** Path ใน .mcp.json ชี้ไปที่ Node version เก่า

**วิธีแก้:** อัพเดท path

```bash
# หา path ใหม่
npm root -g

# อัพเดท .mcp.json ให้ชี้ไปที่ path ใหม่
# เช่น: C:\Users\chitp\AppData\Roaming\nvm\v25.1.0\node_modules\...
```

### Supabase access token หมดอายุ

**อาการ:** Supabase MCP ไม่ทำงาน, error เกี่ยวกับ authentication

**วิธีแก้:**
1. ไปที่ Supabase Dashboard
2. สร้าง access token ใหม่
3. อัพเดทใน `.mcp.json`

---

## 📋 Checklist หลังเปิดเครื่อง

ทำตามนี้ทุกครั้ง:

- [ ] รัน `bash check-mcp.sh`
- [ ] ถ้ามี error ให้แก้ตาม script
- [ ] Reload Claude Code
- [ ] ทดสอบ MCP ทั้ง 4 ตัว

**เวลาทั้งหมด:** ~3 นาที

---

## 🆘 ยังแก้ไม่ได้?

ดูเอกสารฉบับเต็มที่: `docs/MCP_SETUP_GUIDE.md`

หรือถามใน Claude Code โดยแนบ:
- Output จาก `bash check-mcp.sh`
- เนื้อหาของ `.mcp.json`
