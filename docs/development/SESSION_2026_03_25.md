# Session Context — 2026-03-25

## Project Overview
โปรเจค **The Bliss Massage at Home** — แพลตฟอร์มจองบริการนวด สปา ทำเล็บถึงบ้าน
- Monorepo: `apps/customer`, `apps/staff`, `apps/hotel`, `apps/admin`, `apps/server`
- Stack: React + Vite + TypeScript + Supabase + Omise (payment)
- Deploy: Vercel (frontend) + Supabase (DB + Auth)
- Branch หลัก: `feature/customer` (sync กับ `main`)

---

## สิ่งที่ทำในเซสชัน 25 มี.ค. 2569

### ไม่มี code changes — เป็นเซสชันวางแผนและประเมินงาน

1. **อ่าน memory + session context ก่อนหน้า** — ยืนยันสถานะโปรเจค
2. **ตรวจสอบ branch** — `feature/customer` sync กับ `main` ทั้ง local/remote ✅
3. **ประเมิน Man-Hours สำหรับ "ระบบสมาชิกสะสมแต้ม"** — feature ใหม่ที่ user ต้องการ

---

## สถานะปัจจุบัน (ไม่เปลี่ยนจากเซสชัน 24 มี.ค.)

### Branch
```
feature/customer = main = origin/main = origin/feature/customer
commit: 9228d95 "chore: trigger customer app rebuild on Vercel"
```

### Untracked Files (ยังไม่ commit)
```
docs/DB_MIGRATION_DIFF.md     — เปรียบเทียบ migration vs DB จริง
docs/DB_SCHEMA.md             — DB schema ทั้งหมด 54 tables
docs/SESSION_2026_03_23.md    — session context 23 มี.ค.
docs/SESSION_2026_03_24.md    — session context 24 มี.ค.
docs/SESSION_2026_03_25.md    — session context 25 มี.ค. (ไฟล์นี้)
docs/SESSION_CONTEXT.md       — session context เก่า
supabase/migrations/20260323000000_fix_staff_skills_rls_policies.sql
```

### Git Stash
```
stash@{0}: session-2026-03-23: all fixes (jobService, BookingWizard, Profile, Register, payment)
stash@{1}-{8}: stash เก่าจาก branch อื่นๆ
```
**หมายเหตุ:** stash@{0} มี fix `acceptJob()` (profile_id vs staff_id) — DB ที่ restore (17 มี.ค.) ทำงานได้โดยไม่ต้องใช้ fix นี้

---

## Production URLs & Test Accounts

| App | URL | Username | Password |
|-----|-----|----------|----------|
| Customer | https://customer.theblissmassageathome.com | mazmakerdesign@gmail.com | U9B*B2LE#8-q!m8 |
| Hotel | https://hotel.theblissmassageathome.com | reservations@hilton.com | Hotel123. |
| Admin | https://admin.theblissmassageathome.com | admintest@theblissathome.com | Admin@12345 |
| Staff | https://staff.theblissmassageathome.com | LINE LIFF auto-login (ทดสอบ999) | — |

### Dev Ports (localhost)
| App | Port |
|-----|------|
| Admin | 3001 |
| Customer | 3008 |
| Hotel | 3003 |
| Staff | 3004 |
| Server | 3000 |

---

## Features ที่ทำแล้ว

| Feature | สถานะ | หมายเหตุ |
|---------|-------|----------|
| จองล่วงหน้า 3 ชม. (same-day) | ✅ ทำแล้ว + deployed | `BookingWizard.tsx:193` |
| หมายเหตุเพิ่มเติมในการจอง | ✅ ทำแล้ว | `BookingWizard.tsx:72` notes → customer_notes |
| ข้อมูลเซลล์โรงแรม | ✅ ทำแล้ว | DB: `hotels.recommended_sales_staff` |
| LINE error logging (quota/auth) | ✅ ทำแล้ว + deployed | `lineService.ts` |
| Full Booking Flow | ✅ ทดสอบผ่าน | Customer → Staff → Complete |

---

## Features ที่ต้องทำต่อ (ประเมิน Man-Hours แล้ว)

### ง่าย (3-6 ชม.)
| # | Feature | Man-Hours |
|---|---------|-----------|
| 1 | บุคคลอ้างอิง Staff (ชื่อ, เบอร์, สำเนาบัตร) ใน KYC | 4-6 |
| 2 | ส่งบิลทางอีเมล (PDF/HTML invoice + Resend API) | 4-6 |
| 3 | Consent เงื่อนไขคืนเงินหน้าลงทะเบียน | 3-5 |

### ปานกลาง (8-20 ชม.)
| # | Feature | Man-Hours |
|---|---------|-----------|
| 4 | เพิ่มเวลาบริการ (extend session + จ่ายเพิ่ม + หมอคนเดิม) | 12-16 |
| 5 | KYC เอกสาร Staff (upload + Admin approve/reject) | 8-12 |
| 6 | Staff เลือกรอบจ่ายเงิน (15 วัน / เดือน) + แจ้ง Admin | 10-14 |
| 7 | แจ้งเตือนเครดิต 30 วัน โรงแรม + Calendar view + Google Calendar | 14-20 |

### สูงมาก (27-60 ชม.)
| # | Feature | Man-Hours |
|---|---------|-----------|
| 8 | จ่ายเงิน Staff อัตโนมัติ (Bank API → auto transfer) | 30-40 |
| 9 | Livechat แปลภาษาอัตโนมัติ (Real-time + Translation API) | 40-60 |
| 10 | **ระบบสมาชิกสะสมแต้ม** (DB + Backend + Customer UI + Admin UI + Notifications) | **27-38** |

**รวมทั้งหมด: ~152-217 ชม.**

### รายละเอียดระบบสะสมแต้ม (#10)
| ส่วน | Man-Hours |
|------|-----------|
| Database Schema (tiers, points, transactions, rewards) | 2-3 |
| Backend Logic (คำนวณ/แลก/หมดอายุ/tier upgrade) | 8-12 |
| Customer App UI (แต้ม, ประวัติ, แลกรางวัล, checkout ส่วนลด) | 6-8 |
| Admin App UI (จัดการ tier, rules, rewards, manual adjust) | 6-8 |
| Notification (LINE + In-app: ได้แต้ม, ใกล้หมดอายุ, tier เปลี่ยน) | 2-3 |
| Testing & Integration | 3-4 |

**คำถามที่ยังไม่ได้ตอบ (ต้องถาม user ก่อนเริ่มพัฒนา):**
1. กี่ระดับ tier? สิทธิประโยชน์แต่ละระดับ?
2. แต้มหมดอายุไหม? กี่เดือน?
3. แลกแต้มเป็นอะไรได้? (ส่วนลด / บริการฟรี / ของรางวัล)
4. Hotel booking สะสมแต้มด้วยไหม?
5. Referral bonus — แนะนำเพื่อนได้แต้มเพิ่มไหม?

---

## Known Issues

### 1. LINE OA Quota Exceeded
- Free Plan จำกัด 200 ข้อความ/เดือน — หมดแล้ว
- **แก้ไข:** อัปเกรดเป็น Light Plan (200 บาท/เดือน = 5,000 ข้อความ) หรือรอ 1 เม.ย.

### 2. acceptJob() Profile ID vs Staff ID
- Fix อยู่ใน `git stash@{0}` — DB restore (17 มี.ค.) ทำงานได้โดยไม่ต้อง fix
- เก็บไว้ใช้เมื่อ DB schema เปลี่ยน

### 3. DB Migration Drift (59% tables ไม่มี migration)
- รายละเอียดใน `docs/DB_MIGRATION_DIFF.md`
- Technical debt ที่ยังไม่ได้แก้

### 4. pnpm-lock.yaml
- ต้อง `pnpm install --no-frozen-lockfile` แล้ว commit ก่อน push ถ้า lockfile ไม่ตรง

### 5. LIFF Endpoint URL
- Production: `https://staff.theblissmassageathome.com/staff/login`
- ถ้าเคยเปลี่ยนเป็น tunnel URL → ต้องเปลี่ยนกลับที่ LINE Developer Console

---

## MCP Servers
| Server | หมายเหตุ |
|--------|----------|
| supabase | project: theblissathome, ref: rbdvlfriqjnwpxmmgisf |
| vercel-deploy | ชื่อ "vercel-deploy" (ห้ามใช้ "vercel") |
| context7 | Library docs lookup |
| shadcn-ui / shadcn | UI components |
| playwright | ต้องปิด Chrome ก่อนใช้ |

---

## Quick Start สำหรับเซสชันถัดไป

```
อ่านไฟล์ docs/SESSION_2026_03_25.md เพื่อเข้าใจบริบทงานที่ทำค้างไว้ แล้วทำงานต่อ
```
