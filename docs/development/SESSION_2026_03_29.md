# Session Context — 2026-03-29

## Project Overview
โปรเจค **The Bliss Massage at Home** — แพลตฟอร์มจองบริการนวด สปา ทำเล็บถึงบ้าน
- Monorepo: `apps/customer`, `apps/staff`, `apps/hotel`, `apps/admin`, `apps/server`
- Stack: React + Vite + TypeScript + Supabase + Omise (payment)
- Deploy: Vercel (frontend) + Supabase (DB + Auth)

---

## สิ่งที่ทำในเซสชัน 29 มี.ค. 2569

### 1. Commit + Push + Deploy สำเร็จ
**Commit:** `d65f23e` → merge เข้า main → deploy ทุก app สำเร็จ

**3 Features ใหญ่:**
- **KYC เอกสารทะเบียนบ้าน + บุคคลอ้างอิง** (9 ไฟล์)
- **Invoice Email ส่งใบแจ้งหนี้ทางอีเมล** (6 ไฟล์)
- **Refund Policy Consent เงื่อนไขการคืนเงิน** (5 ไฟล์)

### 2. Hotfix: API URL สำหรับ Invoice Email
**Commit:** `ed3f5de` — แก้ fallback จาก `localhost:3000` → `https://the-bliss-at-home-server.vercel.app` เมื่อ PROD=true

**ไฟล์:** `HotelBilling.tsx`, `InvoiceDetailModal.tsx`

### 3. Cache Bust
**Commit:** `b4e9959` — เพิ่ม comment ใน `main.tsx` เพื่อ bust Vercel build cache

### 4. เปลี่ยน Refund Policy จาก Markdown เป็น Plain Text
**Commit:** `be6962b`
- Admin Settings: ลบ "(Markdown)" label, ใช้ textarea ธรรมดา
- Customer RefundPolicyConsent: เปลี่ยนจาก dangerouslySetInnerHTML → whitespace-pre-wrap plain text
- **Admin ต้องแก้เนื้อหาใน Settings ให้เป็นข้อความธรรมดา** (ลบ ##, ###, - ที่เป็น markdown syntax ออก)

### 5. ทดสอบ Production
**ผลลัพธ์:**

| Section | ผลลัพธ์ |
|---------|---------|
| Regression (login ทุก app + dashboard) | ✅ PASS |
| KYC + Emergency Contact (Staff + Admin) | ✅ PASS |
| Refund Policy Consent (Modal + scroll + checkbox) | ✅ PASS |
| Invoice Email (ส่งอีเมลจริง + เนื้อหาถูกต้อง) | ✅ PASS |
| Invoice Email PDF attachment | ⚠️ รอ CDN cache expire |
| Refund Policy plain text | ⚠️ รอ CDN cache expire |

---

## สถานะปัจจุบัน

### Branch
```
feature/staff = main = origin/main
commit: be6962b "refactor: change refund policy from markdown to plain text editor"
ไม่มี uncommitted changes
```

### Commits ในเซสชันนี้
1. `d65f23e` — feat: 3 features หลัก (KYC + Invoice Email + Refund Consent)
2. `ed3f5de` — fix: API URL fallback production
3. `b4e9959` — chore: bust Vercel build cache
4. `be6962b` — refactor: เปลี่ยน refund policy จาก markdown เป็น plain text
5. `00c0610` — fix: แก้ duplicate /api/api/ ใน invoice email URL path

### Vercel Deployment
- Admin deploy commit `00c0610` สำเร็จ (READY + PROMOTED)
- **CDN cache expire แล้ว** สำหรับ commit `be6962b` (Customer: `Co2PaTIg`, Admin: `B4mxgrI0`)
- **ต้องรอ CDN cache expire อีกครั้ง** สำหรับ commit `00c0610` (fix duplicate /api/api/)

---

## งานที่ต้องทำต่อ

### 1. ⭐ ทดสอบ Invoice Email บน Production หลัง CDN cache expire (commit 00c0610)
```
ขั้นตอน:
1. เปิด admin production → hard refresh (Ctrl+Shift+R)
2. ตรวจสอบ JS hash ใน console ต้องเปลี่ยนจาก B4mxgrI0
3. ไป Hotels → เลือกโรงแรม (2fe07be0...) → Billing → กดส่งอีเมล
4. ตรวจสอบ network request ว่าเรียก /api/invoices/... (ไม่ซ้ำ /api/api/)
5. ตรวจสอบว่า:
   - ไม่แสดง "Not Found" error
   - toast แสดง "ส่งใบแจ้งหนี้ไปยัง xxx เรียบร้อย"
   - อีเมลมี PDF attachment
```

### 2. Admin แก้เนื้อหาเงื่อนไขการคืนเงินเป็น plain text
```
Admin → Settings → เงื่อนไขการคืนเงิน → แก้เนื้อหาเป็นข้อความธรรมดา
ลบ markdown syntax (##, ###, -) ที่เหลืออยู่ → บันทึก
แล้วไป customer production → ดู consent modal ว่าแสดงถูกต้อง
```

### 2. Features ที่ยังไม่ได้ทำ (จาก session ก่อนหน้า)
```
ประเมิน Man-Hours แล้ว:
- บุคคลอ้างอิง Staff (สำเนาบัตร) — 4-6 ชม.  [ทำแล้ว ✅]
- ส่งบิลทางอีเมล — 4-6 ชม.  [ทำแล้ว ✅]
- Consent เงื่อนไขคืนเงิน — 3-5 ชม.  [ทำแล้ว ✅]
- เพิ่มเวลาบริการ (extend session) — 12-16 ชม.
- KYC เอกสาร Staff (upload + approve) — 8-12 ชม.  [ทำแล้ว ✅]
- Staff เลือกรอบจ่ายเงิน — 10-14 ชม.
- แจ้งเตือนเครดิต 30 วัน โรงแรม + Calendar — 14-20 ชม.
- จ่ายเงิน Staff อัตโนมัติ — 30-40 ชม.
- Livechat แปลภาษา — 40-60 ชม.
- ระบบสมาชิกสะสมแต้ม — 27-38 ชม.
```

---

## Production URLs & Test Accounts

| App | URL |
|-----|-----|
| Customer | https://customer.theblissmassageathome.com |
| Staff | https://staff.theblissmassageathome.com |
| Hotel | https://hotel.theblissmassageathome.com |
| Admin | https://admin.theblissmassageathome.com |

| App | Username | Password |
|-----|----------|----------|
| Customer | mazmakerdesign@gmail.com | U9B*B2LE#8-q!m8 |
| Hotel | reservations@hilton.com | Hotel123. |
| Admin | admintest@theblissathome.com | Admin@12345 |
| Staff | LINE LIFF auto-login (ทดสอบ999) | — |

---

## Known Issues

### 1. Vercel CDN Cache
- Admin app JS bundle อาจ cached อยู่ที่ CDN edge
- Invoice email จาก UI จะทำงานหลัง cache expire
- ตรวจสอบโดยดู JS file hash ใน console — ต้องเปลี่ยนจาก `DESoMQOP`

### 2. LINE OA Quota
- Free Plan 200 ข้อความ/เดือน หมดแล้ว — รอ 1 เม.ย. หรืออัปเกรด Light Plan

### 3. LIFF Endpoint URL
- Production: `https://staff.theblissmassageathome.com/staff/login`
- ถ้าเคยเปลี่ยนเป็น tunnel URL → เปลี่ยนกลับ

---

## Quick Start

```
อ่านไฟล์ docs/SESSION_2026_03_29.md เพื่อเข้าใจบริบทงาน แล้วทำงานต่อ
```

### Start Dev Servers
```bash
cd /c/chitpon59/dev/project/theblissathome.com/the-bliss-at-home
nohup npx turbo run dev --parallel > /tmp/bliss-dev.log 2>&1 &
```

---

## MCP Servers
| Server | หมายเหตุ |
|--------|----------|
| supabase | project: theblissathome, ref: rbdvlfriqjnwpxmmgisf |
| vercel-deploy | ชื่อ "vercel-deploy" (ห้ามใช้ "vercel") |
| context7 | Library docs lookup |
| shadcn-ui / shadcn | UI components |
| playwright | ต้องปิด Chrome ก่อนใช้ |
