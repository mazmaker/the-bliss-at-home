# Session Context — 2026-03-30

## Prompt สำหรับ Session ใหม่

```
cd C:\chitpon59\dev\project\theblissathome.com\the-bliss-at-home
อ่านไฟล์ docs/SESSION_2026_03_30.md เพื่อเข้าใจบริบทงานทั้งหมด แล้วทำงานต่อ
```

---

## Project Overview
โปรเจค **The Bliss Massage at Home** — แพลตฟอร์มจองบริการนวด สปา ทำเล็บถึงบ้าน
- Monorepo: `apps/customer`, `apps/staff`, `apps/hotel`, `apps/admin`, `apps/server`
- Stack: React + Vite + TypeScript + Supabase + Omise (payment)
- Deploy: Vercel (frontend) + Supabase (DB + Auth)

---

## สิ่งที่ทำในเซสชัน 30 มี.ค. 2569

### Feature หลัก: Hotel Credit 30 วัน + แจ้งเตือน + Calendar View

พัฒนา 4 Phase ครบทั้งหมด:

| Phase | รายละเอียด | สถานะ |
|-------|-----------|-------|
| 1 | DB Migration + Admin Credit Settings + Hotel Dashboard Widget | DONE |
| 2 | Cron Job (09:00 ICT) + Email + In-app Notification + Duplicate Prevention | DONE |
| 3 | Admin Calendar View + Sidebar Menu + Day Detail Modal | DONE |
| 4 | Google Calendar API Integration + Admin Settings + Setup จริง | DONE |

### Bugfix ระหว่าง session:
1. **credit_cycle_day auto-fill** — เปลี่ยนจาก Admin กรอกเอง → คำนวณอัตโนมัติจาก `credit_start_date + credit_days` (แก้ไขได้)
2. **Due date calculation** — แก้จากการหา `credit_cycle_day` ถัดไปในเดือน → ใช้ `credit_start_date + credit_days` ที่ถูกต้อง
3. **Hotel user notification** — แก้ query จาก `profiles.hotel_id` → `hotels.auth_user_id`
4. **Hotels list credit display** — เพิ่มช่อง "เครดิต (วัน)" ใน stats grid ของ Hotels list

---

## ไฟล์ที่แก้ไข/สร้างใหม่ (ทั้งหมด)

### ไฟล์ใหม่ (NEW):
| ไฟล์ | รายละเอียด |
|------|-----------|
| `apps/admin/src/pages/CreditCalendar.tsx` | หน้าปฏิทินเครดิต — Calendar grid, stats, modal |
| `apps/server/src/services/googleCalendarService.ts` | Google Calendar API — create/update/delete events |
| `docs/REPORT_HOTEL_CREDIT_SYSTEM.md` | รายงานการเปลี่ยนแปลงอย่างละเอียด |
| `docs/TEST_PLAN_HOTEL_CREDIT.md` | แผนทดสอบ 25 Test Cases + Prompt สำหรับ Claude AI |

### ไฟล์ที่แก้ไข (MODIFIED):
| ไฟล์ | รายละเอียด |
|------|-----------|
| `apps/admin/src/pages/HotelDetail.tsx` | เพิ่ม credit section + แก้ due date calculation |
| `apps/admin/src/components/HotelForm.tsx` | เพิ่ม 3 credit fields + auto-fill credit_cycle_day |
| `apps/admin/src/pages/Hotels.tsx` | เพิ่มช่อง "เครดิต (วัน)" ใน stats grid |
| `apps/admin/src/App.tsx` | เพิ่ม route `/admin/credit-calendar` |
| `apps/admin/src/layouts/AdminLayout.tsx` | เพิ่มเมนู "ปฏิทินเครดิต" ใน sidebar |
| `apps/admin/src/pages/Settings.tsx` | เพิ่ม Google Calendar config section |
| `apps/hotel/src/hooks/useHotelContext.ts` | เพิ่ม credit fields ใน HotelData + select query |
| `apps/hotel/src/pages/Dashboard.tsx` | เพิ่ม Credit Widget + แก้ due date calculation |
| `apps/server/src/index.ts` | เพิ่ม cron `0 2 * * *` (09:00 ICT) + import |
| `apps/server/src/services/notificationService.ts` | เพิ่ม `processCreditDueReminders()` |
| `apps/server/src/services/emailService.ts` | เพิ่ม `creditDueReminderEmailTemplate()` |

### Database (via Supabase MCP):
```sql
-- hotels table: 3 columns เพิ่ม
ALTER TABLE hotels ADD COLUMN credit_days INTEGER DEFAULT 30;
ALTER TABLE hotels ADD COLUMN credit_start_date DATE;
ALTER TABLE hotels ADD COLUMN credit_cycle_day INTEGER;

-- hotel_credit_notifications table: ใหม่
CREATE TABLE hotel_credit_notifications (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  hotel_id UUID NOT NULL REFERENCES hotels(id),
  bill_id UUID REFERENCES monthly_bills(id),
  notification_type TEXT NOT NULL,
  channel TEXT NOT NULL,
  sent_at TIMESTAMPTZ,
  status TEXT DEFAULT 'pending',
  created_at TIMESTAMPTZ DEFAULT now()
);
```

### Package:
- `googleapis` added to `@bliss/server`

---

## Google Calendar Setup (ทำจริงแล้ว)

| รายการ | ค่า |
|--------|-----|
| Google Cloud Project | billboardmap (billboardmap-395702) |
| Service Account | bliss-calendar@billboardmap-395702.iam.gserviceaccount.com |
| JSON Key | billboardmap-395702-c5192f8b9a58.json (อยู่ใน .playwright-mcp/) |
| Calendar Name | Bliss Credit Reminders |
| Calendar ID | ed13fc04bec6ed2c9f28b820983b1b462dadb753465e8ab222b93e7d68ac90bd@group.calendar.google.com |
| Permission | แก้ไขกิจกรรม (Writer) |
| Config | บันทึกใน Admin Settings → การชำระเงิน → Google Calendar แล้ว |

---

## ผลทดสอบ

### TC ที่ผ่านแล้ว:
| TC | ชื่อ | ผลลัพธ์ |
|----|------|---------|
| 01 | Admin ตั้งค่าเครดิตโรงแรม | PASS |
| 02 | Admin Hotel Detail แสดงเครดิต | PASS |
| 03 | Admin แก้ไขรอบเครดิต | PASS |
| 05 | Hotel Dashboard Credit Widget | PASS |
| 07 | ปฏิทินเครดิต — แสดงข้อมูล | PASS |
| 08 | Badge วันที่ครบกำหนด | PASS |
| 09 | คลิกวันที่ดู Modal | PASS |
| 10 | เลื่อนเดือน + ปุ่มวันนี้ | PASS |
| 11 | Cron Credit Due Reminder | PASS |
| 12 | Duplicate Prevention | PASS |
| 14 | In-app Notification Hotel App | PASS |
| 16 | Email ส่งจริง (Resend API) | PASS |
| 17 | Google Calendar Config | PASS |
| 24 | Regression Login | PASS |

### TC ที่ยังไม่ได้ทดสอบ:
| TC | ชื่อ |
|----|------|
| 04 | Validation ค่าเครดิต (min/max) |
| 06 | ไม่แสดง Widget ถ้าไม่ตั้งค่า |
| 13 | Credit Overdue (ครบกำหนดวันนี้) |
| 15 | In-app notification Admin App |
| 18 | ลบ Google Calendar Config |
| 19 | ไม่ส่งถ้าโรงแรม inactive |
| 20 | โรงแรมไม่มี email |
| 21 | หลายโรงแรมครบกำหนดวันเดียวกัน |
| 22 | สีตาม urgency (3 กรณี) |
| 23 | Google Calendar สร้าง Event |

---

## สถานะปัจจุบัน

### Branch & Commits
```
branch: feature/staff = main = origin/main
commit ล่าสุดที่ push: 00c0610
ยังไม่ได้ commit งาน Hotel Credit (ทั้งหมดเป็น uncommitted changes)
```

### Dev Servers
```bash
# กำลังรันอยู่แล้ว
cd /c/chitpon59/dev/project/theblissathome.com/the-bliss-at-home
nohup npx turbo run dev --parallel > /tmp/bliss-dev.log 2>&1 &

# Ports:
# Admin: http://localhost:3001
# Hotel: http://localhost:3003
# Server: http://localhost:3000
# Customer: http://localhost:3008
# Staff: http://localhost:3004
```

### Test Data ปัจจุบันใน DB:
- โรงแรมดุสิต เชียงใหม่: credit_days=30, credit_start_date=2026-04-01, credit_cycle_day=1
- มี test notifications จาก cron trigger อยู่ใน DB (สามารถลบได้)
- Google Calendar config บันทึกใน settings table แล้ว

---

## งานที่ต้องทำต่อ (ตามลำดับ)

### 1. ทดสอบ TC ที่เหลือ (TC-04, 06, 13, 15, 18-23)
```
ใช้ Prompt จาก docs/TEST_PLAN_HOTEL_CREDIT.md
ทดสอบผ่าน Playwright MCP
ถ้า FAIL → แก้ไขโค้ด → ทดสอบซ้ำ
```

### 2. Commit + Push + Deploy
```
เมื่อทดสอบผ่านทั้งหมด:
- git add ไฟล์ที่เกี่ยวข้อง
- git commit -m "feat: hotel credit 30-day system with cron notifications, calendar view, and Google Calendar integration"
- git push
- ตรวจ Vercel deploy
```

### 3. ทดสอบ Production
```
- Admin: https://admin.theblissmassageathome.com
- Hotel: https://hotel.theblissmassageathome.com
- ตรวจ CDN cache (อาจต้องรอ expire)
```

### 4. งานค้างจาก session ก่อนหน้า
```
- ทดสอบ Invoice Email + PDF หลัง CDN cache expire (commit 00c0610)
- Admin แก้เนื้อหาเงื่อนไขเป็น plain text
```

---

## Production URLs & Test Accounts

| App | URL |
|-----|-----|
| Customer | https://customer.theblissmassageathome.com |
| Staff | https://staff.theblissmassageathome.com |
| Hotel | https://hotel.theblissmassageathome.com |
| Admin | https://admin.theblissmassageathome.com |

| App | Username | Password |
|-----|----------|----------|
| Customer | mazmakerdesign@gmail.com | U9B*B2LE#8-q!m8 |
| Hotel (ดุสิต เชียงใหม่) | mazmakerv3.sup@gmail.com | Hotel123. |
| Admin | admintest@theblissathome.com | Admin@12345 |
| Staff | LINE LIFF auto-login (ทดสอบ999) | — |

---

## MCP Servers
| Server | หมายเหตุ |
|--------|----------|
| supabase | project: theblissathome, ref: rbdvlfriqjnwpxmmgisf |
| vercel-deploy | ชื่อ "vercel-deploy" (ห้ามใช้ "vercel") |
| context7 | Library docs lookup |
| shadcn-ui / shadcn | UI components |
| playwright | ต้องปิด Chrome ก่อนใช้ |

---

## เอกสารที่เกี่ยวข้อง
- `docs/PLAN_HOTEL_CREDIT_NOTIFICATION.md` — แผนพัฒนาเดิม (Requirement + Design)
- `docs/REPORT_HOTEL_CREDIT_SYSTEM.md` — รายงานการเปลี่ยนแปลงอย่างละเอียด
- `docs/TEST_PLAN_HOTEL_CREDIT.md` — แผนทดสอบ 25 TC + Prompt
- `docs/SESSION_2026_03_29.md` — บริบท session ก่อนหน้า
