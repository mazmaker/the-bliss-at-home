# Session Context — 2026-04-03

## Prompt สำหรับแชทใหม่

```
cd C:\chitpon59\dev\project\theblissathome.com\the-bliss-at-home

## บริบทโปรเจค

Monorepo "The Bliss at Home" — แพลตฟอร์มจองนวด/สปา
- 5 Apps: Admin (:3001), Customer (:3008), Hotel (:3003), Staff (:3004), Server (:3000)
- Stack: React 18, Vite, TypeScript, Tailwind, Express, Supabase, LINE LIFF
- Branch: feature/staff (ตรงกับ origin/main)

## สิ่งที่ทำเสร็จแล้วใน Session นี้ (2026-04-03)

### Staff Payout Schedule System — เสร็จสมบูรณ์ 6 Phases

ระบบรอบจ่ายเงิน Staff แบบอัตโนมัติ:
- Staff เลือกรอบ ครึ่งเดือน/รายเดือน ในหน้า Settings
- Server Cron ตัดรอบอัตโนมัติ วันที่ 10 + 25 ทุกเดือน (08:00 ICT)
- Carry forward ยกยอดอัตโนมัติเมื่อต่ำกว่าขั้นต่ำ
- Force payout สำหรับ Staff inactive
- Notifications: แจ้งล่วงหน้า 1 วันก่อนตัดรอบ/จ่ายเงิน + duplicate prevention
- Admin Payout Dashboard: Stats, ตาราง (รวม virtual rows ยกยอด/ยังไม่ถึงรอบ), Filter, Batch payout + LINE notification, Bank warning, Export CSV
- Admin Settings: tab ตั้งค่ารอบจ่ายเงิน + validation (1-28, 0-100000)
- Staff Earnings: แสดง payout_round label + payout jobs detail + carry forward info

### Commits (feature/staff branch)
- 5920638 — feat(payout): add staff payout schedule system (Phase 1-6)
- 7aedd22 — feat(payout): validation, LINE push, bank warning, earnings integration
- 0ef6601 — feat(payout): force payout inactive, payout jobs detail, carry forward stats
- a211a34 — feat(payout): carry-forward and not-due virtual rows in dashboard
- 3264bc8 — fix(earnings): usePayouts use profileId not staffId
- 4696fac — fix(payout): bank account check uses staff.id not profile_id
- cca2b1e — docs: update all docs with implementation report

### DB Migration (via Supabase MCP)
- staff table: เพิ่ม payout_schedule column
- สร้าง payout_settings table (6 settings)
- payouts table: เพิ่ม payout_round, is_carry_forward, carry_forward_amount
- สร้าง payout_notifications table (duplicate prevention)
- RLS policies ครบ

### ไฟล์สำคัญ
- apps/admin/src/pages/PayoutDashboard.tsx (ใหม่)
- apps/server/src/services/payoutService.ts (ใหม่)
- apps/staff/src/pages/StaffSettings.tsx (แก้ไข)
- apps/staff/src/pages/StaffEarnings.tsx (แก้ไข)
- apps/admin/src/pages/Settings.tsx (แก้ไข)
- packages/supabase/src/earnings/earningsService.ts (แก้ไข)
- packages/supabase/src/earnings/useEarnings.ts (แก้ไข)
- Dev endpoint: POST /api/dev/trigger-payout-cutoff (รับ {"date":"2026-04-10"})

### ผลทดสอบ
- 106/108 TC PASS (98%), 0 FAIL
- 2 N/A: Email notification (TC-45/46) — ยังไม่ได้เชื่อมต่อ emailService
- Test Plan: docs/TEST_PLAN_STAFF_PAYOUT_SCHEDULE.md (108 TCs, 8 Parts A-H)

### Bugs ที่พบและแก้แล้ว
1. toISOString() timezone offset → ใช้ formatDate() local time
2. payout_jobs column tip ไม่มี → ลบออก
3. null profile_id UUID error → filter .not('profile_id', 'is', null)
4. .onConflict() not a function → .upsert({}, { onConflict })
5. Admin Dashboard import supabase ผิดตัว → ../lib/supabase
6. setMessage({object}) crash → string
7. usePayouts ใช้ staff.id แต่ payouts.staff_id = profile_id → ใช้ profileId
8. bank_accounts.staff_id = staff.id ไม่ใช่ profile_id → lookup ผ่าน staff table

### FK Relationship ที่ต้องระวัง
- jobs.staff_id → profiles.id (ไม่ใช่ staff.id)
- payouts.staff_id → profiles.id
- bank_accounts.staff_id → staff.id (ไม่ใช่ profiles.id)
- payout_jobs.job_id → jobs.id
- payout_notifications.staff_id → profiles.id

## สิ่งที่ยังไม่ได้ทำ / ทำต่อ

1. **Email notification สำหรับ payout** (TC-45/46) — เชื่อมต่อ emailService
2. **Deploy production** — merge feature/staff → main → Vercel auto-deploy
3. **TC ระดับ Module ครอบคลุมทั้งแพลตฟอร์ม** (~570 TCs, 35 modules)
   — ดู docs/TEST_PLAN_STAFF_PAYOUT_SCHEDULE.md เป็นตัวอย่าง format
4. **ฟีเจอร์ถัดไป** — ดู docs/CHECKLIST.md สำหรับ remaining work

## กฎการทดสอบ (สำคัญมาก)

1. ห้ามใช้ "โค้ดรองรับ" เป็นผลทดสอบ — ต้องเห็นผลจริงบน UI ผ่าน Playwright
2. ทุก TC ต้องทดสอบทั้ง positive และ negative case
3. ก่อนเขียน query ที่ใช้ FK — ตรวจ DB ก่อนว่า column อ้างอิงตารางไหน
4. ทดสอบกับข้อมูลจริงในระบบ ไม่ใช่แค่ test data ที่สร้างเอง
5. เมื่อทดสอบเสร็จ ให้ทำ sanity check รอบสุดท้าย เปิดทุกหน้าที่แก้ไข

## Login Credentials
- Admin: admintest@theblissathome.com / Admin@12345
- Customer: mazmakerdesign@gmail.com / U9B*B2LE#8-q!m8
- Hotel: reservations@hilton.com / Hotel123.
- Staff: LINE LIFF auto-login (ทดสอบ999) ผ่าน cloudflare tunnel

## เริ่มงาน
อ่าน memory + CLAUDE.md + docs/CHECKLIST.md แล้วบอกฉันว่าจะทำอะไรต่อ
```
